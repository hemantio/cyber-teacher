'use client';

// SVG Overlay - Currently disabled to prevent duplicate circles
// The Canvas already handles entity rendering with status indicators

export function SvgOverlay() {
    // Returning null - all rendering handled by Canvas
    return null;
}
